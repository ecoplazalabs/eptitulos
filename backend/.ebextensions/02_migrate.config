container_commands:
  01_migrate:
    command: "source /var/app/venv/*/bin/activate && cd /var/app/staging && python -m alembic upgrade head 2>&1 | tee /tmp/alembic_migrate.log; echo EXIT_CODE=$?"
    leader_only: true
